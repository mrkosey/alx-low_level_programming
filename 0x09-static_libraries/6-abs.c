#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: interger to check
 * Return: absolute value of interger
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
